# Slavic Mind Academy

This project is being built with Lovable Cloud.

## Development

Install dependencies and start the development server:

```bash
bun install
bun run dev
```

## Verification

Pull requests and phase branches are verified with GitHub Actions. The verification workflow runs TypeScript checks, lint on changed TypeScript source, the production build, and browser smoke tests on desktop and 393px mobile.
